# Core dependencies
Pillow>=10.0.0
pytesseract>=0.3.10
pdf2image>=1.16.3
PyPDF2>=3.0.1
pdfplumber>=0.10.0

# Image processing and hashing
imagehash>=4.3.1
opencv-python>=4.8.0

# Data extraction and processing
python-dateutil>=2.8.2
regex>=2023.8.8

# GUI (tkinter is built-in with Python)
# No additional GUI dependencies needed

# Optional: Enhanced OCR
# easyocr>=1.7.0  # Uncomment for better OCR accuracy (larger download)
